// usePermissions hook has been removed.
// All permission decisions are now made directly in AuthContext via actor.isAdmin().
// Consumers should import { useAuth } from "@/contexts/AuthContext" and use isAdmin (boolean).
